"""Placeholder test so the suite is non-empty and CI runs green from Phase 0.

Replaced by real hygiene/leakage/harness tests as the phases land.
"""


def test_placeholder() -> None:
    assert True
